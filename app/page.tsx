import { AppWindow } from "@/components/app-window"

export default function Page() {
  return (
    <main className="flex h-screen w-screen overflow-hidden bg-transparent">
      <AppWindow />
    </main>
  )
}
